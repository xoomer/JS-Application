-- Client-side script (minimal - queue display is now server-side)
-- All queue UI is handled via server-side adaptive cards during connection

print("^2[QUEUE]^7 Client script loaded!")
print("^2[QUEUE]^7 Queue display uses server-side adaptive cards")
